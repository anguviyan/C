#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct student {
	char *name;
	int age;
}stu={"sriram",10};

int main(void)
{
	struct student *ptr = &stu;
	
	printf("Name of the student is %s\n",ptr->name);
	printf("Age of the student is %d\n",ptr->age);

	return 0;
}
